# 心跳机制（system:heartbeat）通路校验 — 记录 2025-10-30 20:52:12

目的
- 统一三段式契约：`system:heartbeat:requested/completed`。
- 前端 WSClient 能发送/接收；严格校验（含 metadata.version），不做兜底。

已完成
- 常量声明：`src/frontend/common/event/event-constants.js`
  - `HEARTBEAT_REQUESTED`, `HEARTBEAT_COMPLETED`
- WSClient：`src/frontend/common/ws/ws-client.js`
  - `VALID_MESSAGE_TYPES` 增加 `system:heartbeat:completed`
  - 新增 `sendHeartbeat(timeout=4000)`，走严格 `request()`，自动注入 `metadata: {version:'1.0.0'}`
- 单测：`src/frontend/common/ws/__tests__/ws-client.heartbeat.test.js`
  - 常量/白名单断言（通过）

建议验证
- 前端运行时手动调用（控制台）：
  - `window.wsClient.sendHeartbeat().then(console.log).catch(console.error)`
- 后端日志路径：
  - `dist/latest/logs/backend-launcher.log`（Hosted）
  - `dist/latest/logs/runtime-ports.json`（端口握手）

后续可选工作（非本次强制）
- 若需要周期心跳：在 `app-container` 或 Viewer/App 入口显式调度（例如每 30s），并提供开关；默认不启用（当前已关闭）。
- 增补端到端集成测试（Stub WebSocket 或本地起后端 WS）。

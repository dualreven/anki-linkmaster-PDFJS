# Working Log（D）

## 目标
- 拆分 `OutlineFeature` 装配层，让 `index.js` 更像 composition root，降低面条化风险。

## 交付格式（完成时必须填写）
- `commit(s)`: `<hash...>`
- `scope`: `src/frontend/pdf-viewer/features/pdf-outline/**`
- `tests`: `pnpm -s run lint` + `pnpm exec jest --runTestsByPath ... -i`
- `notes`: `<是否触碰跨模块契约/是否需要人工点检>`


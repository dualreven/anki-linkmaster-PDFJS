# Working Log（C）

## 目标
- `AnnotationManager V2` 变成纯逻辑/纯 store；事件发射集中到 `AnnotationFeature`。

## 交付格式（完成时必须填写）
- `commit(s)`: `<hash...>`
- `scope`: `src/frontend/pdf-viewer/features/pdf-annotation/**`
- `tests`: `pnpm -s run lint` + `pnpm exec jest --runTestsByPath ... -i`
- `notes`: `<是否引入破坏性变更/是否需要人工点检>`


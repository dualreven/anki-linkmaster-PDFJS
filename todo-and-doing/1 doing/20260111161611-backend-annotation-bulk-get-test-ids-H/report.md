# Report - 20260111161611-backend-annotation-bulk-get-test-ids-H

## Scope
- （必填）允许修改/新增（v001 scope 内）：后端 `annotation:bulk-get` + DB seed + 对应测试文件

## Commands & Results
- `pnpm -s run lint`：（✅/❌ + 关键信息）
- 后端定向测试命令：（例如 `python -m pytest ...`）（✅/❌ + 关键信息）

## Deliverable
- Commit: （必填，hash）
- 主要变更点（3~6 条）：

## Notes / Risks
- （可选）已知风险与后续建议：


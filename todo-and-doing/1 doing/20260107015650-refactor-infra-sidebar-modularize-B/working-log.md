# Working Log（B）

## 目标
- 将 `infra-sidebar` 拆分为更小模块，降低“面条化”风险，保持行为不变。

## 交付格式（完成时必须填写）
- `commit(s)`: `<hash...>`
- `scope`: `src/frontend/pdf-viewer/features/infra-sidebar/**`
- `tests`: `pnpm -s run lint` + `pnpm exec jest --runTestsByPath ... -i`
- `notes`: `<是否触碰跨模块契约/是否需要人工点检>`


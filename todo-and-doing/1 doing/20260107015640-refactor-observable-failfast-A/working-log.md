# Working Log（A）

## 目标
- 让 `ObservableState` 对契约违规输入 fail-fast（抛错），并补齐回归测试。

## 交付格式（完成时必须填写）
- `commit(s)`: `<hash...>`
- `scope`: `src/frontend/common/utils/observable.js` + `src/frontend/common/utils/__tests__/observable.test.js`
- `tests`: `pnpm -s run lint` + `pnpm exec jest --runTestsByPath ... -i`
- `notes`: `<是否引入破坏性变更/是否需要人工点检>`


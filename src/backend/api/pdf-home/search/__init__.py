"""Search domain package for pdf-home.

Holds SearchService and related DTO/contracts. Implementations are optional;
`PDFLibraryAPI` will delegate here if a service is registered, otherwise
fallback to its built-in logic to keep backward compatibility.
"""


title: iteration-acceptance
role: 迭代验收
summary: 抽查并确认迭代交付是否满足需求与质量要求，输出验收报告。
inputs:
  - 迭代产物、测试与验证报告
outputs:
  - 1.4 迭代验收报告
steps:
  - 抽查关键交付项并比对验收标准
  - 汇总通过/不通过项并给出改进建议
constraints:
  - 验收记录应包含抽查样本与判定依据
principles:
  - 客观、可复现、追责到人

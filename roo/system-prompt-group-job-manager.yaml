title: Group-Job-Manager
role: 分组任务管理
summary: 根据分组计划调度执行原子任务，汇总通过率、缺陷与工时消耗。
inputs:
  - 相关架构/规范/任务清单
outputs:
  - 1.3 分组任务执行结果（含通过率、缺陷清单、工时）
steps:
  - 按分组顺序调用 Job-Executor 执行原子任务
  - 汇总执行结果并计算指标
constraints:
  - 执行结果需包含日志与可回溯的构建产物
principles:
  - 以数据为准，记录失败原因并形成整改任务
